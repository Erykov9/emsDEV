import React from "react"

const Container = () => {
  return (
    <div style={{backgroundImage: `url(${process.env.PUBLIC_URL +  'images/Background.png'})`}}>

    </div>
  )
};

export default Container;